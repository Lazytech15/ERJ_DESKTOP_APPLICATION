# ERJ Smart Solution – Attendance Management System

A desktop attendance/time-tracking application for monitoring employee clock-ins, clock-outs, working hours, and daily attendance stats — built with a React (Vite) frontend and an Electron backend.

## Purpose

This app is designed for front-desk / kiosk-style use at an office or worksite:

- Employees scan a barcode or enter their ID to clock in/out for the day.
- A live dashboard shows who's currently clocked in, recent clock-outs, and daily stats (regular hours, overtime, present, late, absent).
- Selecting an employee shows their details, current clock state, and a one-tap clock-in/out action.
- Runs as a native desktop app (Windows/macOS/Linux) via Electron, so it can run on a dedicated kiosk machine without a browser.

## Screenshot

![App screenshot](./assets/screenshot.png)

*(Replace `assets/screenshot.png` with an actual screenshot of the running app.)*

## Tech Stack

| Layer        | Technology                                         |
|--------------|-----------------------------------------------------|
| UI            | React 18                                            |
| Build tool    | Vite 5                                              |
| Desktop shell | Electron 38                                         |
| Packaging     | electron-builder (NSIS / DMG / AppImage)            |
| IPC bridge    | Electron `contextBridge` + `ipcRenderer` (preload)  |
| Styling       | Plain CSS (per-component stylesheets, CSS variables)|
| Backend libs  | axios, express, ws, xlsx, crypto-js, zod            |
| Auto-update   | electron-updater + electron-log                     |

## Structure
```
erj-attendance/
├── electron/            # Electron main process (backend)
│   ├── main.js           # Window, IPC handlers, attendance data
│   ├── preload.js         # Secure IPC bridge (window.electronAPI)
│   └── dist/              # Built frontend (generated by `npm run build:frontend`)
├── frontend/             # React + Vite app (UI)
│   ├── index.html
│   ├── vite.config.js
│   └── src/
│       ├── App.jsx
│       ├── main.jsx
│       ├── components/   # Header, Scanner, StatsRow, EmployeeList, DetailCard, ClockOutCard, Titlebar, Icons
│       ├── hooks/         # useClock
│       ├── services/      # api.js (window.electronAPI wrapper)
│       ├── data/          # sample/mock data for browser dev mode
│       ├── styles/        # CSS (per-component)
│       └── utils/
├── assets/
│   └── icon.svg
└── package.json          # root scripts orchestrate frontend + electron
```

## Setup
```bash
npm install
```
This also installs the `frontend/` dependencies (via `postinstall`).

## Development
Runs the Vite dev server and Electron together (hot reload):
```bash
npm run dev
```

## Production build / run
Builds the React app into `electron/dist` and launches Electron against it:
```bash
npm start
```

## Package the desktop app
```bash
npm run dist
```

See [COMMANDS.md](./COMMANDS.md) for the full command reference.

## IPC API (window.electronAPI)
Exposed by `electron/preload.js`:
- `getAttendanceState()` – fetch current employees, clock-outs, stats
- `scanEmployee({ value, mode })` – lookup employee by ID or name
- `clockAction({ employeeId })` – toggle clock in/out for an employee
- `refreshAttendance()` – re-fetch attendance data
- `onAttendanceUpdate(callback)` – subscribe to live state pushes
- `openSettings()` / `openEmployeeDirectory()` – stubs for future modals
- `minimizeWindow()` / `maximizeWindow()` / `closeWindow()` / `getVersion()`

Replace the in-memory demo data in `electron/main.js` with real database/API logic.